[입력 파일]
- 원본: `논문초고_5장.md` — 읽기 전용
- 작업본: `논문초고_6장.md`

[사례]
제4장 `3. RQ1/H1 검정 결과`의 첫 문단은 다음과 같습니다. 현재 문단에는 이어지는 표 5에 대한 참조가 없습니다.

> 연구질문 1은 대학생의 생성형 AI 학습중심 활용이 이후의 자기조절학습을 정적으로 예측하는가에 관한 것이었다. 이를 검정하기 위해 SRL T2를 종속변수로 하고 LCU T1을 주요 예측변수로 설정하였으며, SRL T1과 중간고사 점수를 통제한 다중회귀분석을 실시하였다.

대응하는 표에는 `tbl-rq1-regression` 라벨을 사용할 예정입니다.

[학생 판단]
- 표를 참조할 적절한 위치: {학생이 먼저 작성}
- 사용할 상호참조: {학생이 먼저 작성}
- 본문에서 유지할 핵심 설명: {학생이 먼저 작성}

[요청]
현재 문단의 의미를 유지하면서 라벨 상호참조를 추가하고, 표의 모든 값을 반복하지 않으면서 연구질문에 답하는 문장 구조를 제안하세요.

[수정 규칙]
- 본문에서는 백틱 없이 `@tbl-rq1-regression` 형식으로 참조합니다.
- 표에 없는 수치, 변수, 분석조건과 해석을 추가하지 마세요.
- `영향을 미쳤다`와 같은 인과적 표현으로 바꾸지 마세요.
- 뒤 문단에 이미 보고된 회귀계수와 모형 적합도는 불필요하게 반복하지 마세요.

[출력]
`원문 | 문제 | 수정문 | 대응 라벨 | 유지한 의미`로 정리하고, 제4장 `3. RQ1/H1 검정 결과`에서 교체할 문장만 제시하세요.
