내 논문 프로젝트에서 표와 그림 파일의 연결 상태를 점검해줘.

다음 범위에서 읽기 전용으로 작업해줘.

- `논문 초안.md`에서 표와 그림의 라벨과 파일 경로를 찾는다.
- `tables`와 `figures` 폴더에서 실제 파일의 존재 여부를 확인한다.
- 같은 라벨이 중복되었는지 확인한다.
- 원고에서 사용되지 않는 파일과 원고에는 있지만 찾을 수 없는 파일을 구분한다.

파일을 수정하거나 이동하거나 이름을 바꾸거나 삭제하지 마. 결과는 다음 네 목록으로 보고해줘.

1. 정상적으로 연결된 항목
2. 누락된 파일
3. 중복된 라벨
4. 사용 여부를 연구자가 확인해야 할 파일
